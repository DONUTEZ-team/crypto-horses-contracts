import { zeroAddress } from "../test/helpers/Utils";

import { RandomizerProxyStorage } from "../test/types/RandomizerProxy";

export const randomizerProxyStorage: RandomizerProxyStorage = {
  randomizer: zeroAddress,
  admin: zeroAddress,
  pending_admin: zeroAddress,
};
