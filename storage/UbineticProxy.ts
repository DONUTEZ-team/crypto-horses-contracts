import { zeroAddress } from "../test/helpers/Utils";

import { UbineticProxyStorage } from "../test/types/UbineticProxy";

export const ubineticProxyStorage: UbineticProxyStorage = {
  ubinetic: zeroAddress,
  admin: zeroAddress,
  pending_admin: zeroAddress,
};
